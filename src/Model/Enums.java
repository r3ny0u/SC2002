package Model;
/*
 * I threw all enums here cause it was getting clotted
 */

enum MovieType {
    MOVIE3D,
    MOVIE2D,
}

enum CinemaClass {
    NORMAL,
    PLATINUM,
}

enum Age {
    ADULT,
    SENIOR_CITIZEN,
    CHILD,
}

enum DayOfWeek {
    WEEKDAY,
    WEEKEND,
    PUBLIC_HOLIDAY;
}

enum DataType {
    WEEKDAY_PRICE,
    WEEKEND_PRICE,
    PH_PRICE,
    MOVIE2D,
    MOVIE3D,
    NORMAL,
    PLATINUM,
    ADULT,
    SENIOR_CITIZEN,
    CHILD,
}